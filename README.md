## Getting Started
1. `.env.local`を`api`直下に配置
1. `pnpm i --frozn-lockfile`
1. 以下で起動し
    ```shell
    npm run dev
    ```
1. [http://localhost:8088/docs](http://localhost:8088/docs)から直接API叩きでPDFが落とせるはず

> [!Note]
> Dirに見せるようにWeb画面から叩く実装つける予定でweb設けてますが、まだあんまりまとまってないです。

## 概要
`pdf-lib`の機能検証

- PDFFormを使用したテンプレートへの値埋め込み
- 同上のフォントサブセット化埋め込み
- 同上の画像差し込み

同時に、サービスアカウントを経由したGoogleDriveからの画像取得も検証実装

## 構成

L api : apiv2に実装する想定で作ったサンプル apiv2から持ってきて不要箇所削ったものがベース
L web : 画面呼び出しのサンプル用に初期構成のままのプロジェクト

## 補足

Lobby画面から呼ぶ場合lobbyApiも経由するが、経由するだけであんまり関係ないので省略(予定)
ログ調整などしてないのでDrive疎通失敗時などファイルIDがresで返りうる。本実装時は要調整。

## テンプレート構築処理の概念モデル
[入力]
- テンプレートPDFのバイナリ
- 印影画像
- 入力データ

テンプレートビルダー